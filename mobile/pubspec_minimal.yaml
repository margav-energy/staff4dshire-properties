name: staff4dshire_properties
description: Staff4dshire Properties - Staff and Subcontractor Management App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Essential UI & Navigation
  cupertino_icons: ^1.0.6
  go_router: ^12.1.3
  
  # Essential State Management
  provider: ^6.1.1
  
  # Essential HTTP
  http: ^1.1.2
  
  # Essential Local Storage
  shared_preferences: ^2.2.2
  
  # Essential Location
  geolocator: ^10.1.0
  
  # Essential Permissions
  permission_handler: ^11.1.0
  
  # Essential Date & Time
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

